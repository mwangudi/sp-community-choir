# dependencies
node_modules
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
coverage

# next.js
.next/
out/
build
dist

# misc
.DS_Store
*.pem
.env*.local
.env

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel

# internal planning docs (contain member names — keep out of the repo)
*.xlsx
*.xls
*.pptx

# unoptimised photo drops — the AVIFs in public/gallery are what ship
originals/

# runtime uploads — on the droplet this path is a symlink to /var/lib/choir-uploads
public/uploads/

# large source archives
*.zip

# Build artefacts from deploy/build-bundle.ps1
.deploy-bundle/
choir-bundle.tgz
worship-aid-*.pdf
